{
  "name": "asciigen",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspace @asciigen/engine && npm run build --workspace @asciigen/cli && npm run build --workspace web",
    "lint": "npm run lint --workspace web",
    "test": "npm run build --workspace @asciigen/engine && npm run test --workspace @asciigen/engine"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "ts-node": "^10.9.2",
    "turbo": "latest",
    "typescript": "^6.0.3"
  }
}
